---
title: cli.ts + bin.ts — argparse clone
section: cli
status: placeholder
---

# TPP: cli.ts + bin.ts

# (Wave 4 — PLACEHOLDER. DEPENDS ON index-orchestration. Scope before implementing.)

## Status

**Placeholder** — intentionally light per the roadmap. First `/tpp` action is "Research &
scoping" once index-orchestration lands.

## Summary

Port `ftfy/cli.py` as a hand-rolled argparse clone with exact flags, error texts, and exit
codes, plus the `bin.ts` shebang entry. CLI text is API — tests compare full stdout/stderr.

## Current phase

- [ ] Research & scoping ← start here
- [ ] Write breaking tests (against built `dist/bin.js`)
- [ ] Implementation
- [ ] Final integration verification

## Required reading

- [CLAUDE.md](../CLAUDE.md) — locked decisions, build/test commands
- [docs/TPP-GUIDE.md](../docs/TPP-GUIDE.md) — workflow
- [docs/DESIGN.md](../docs/DESIGN.md) — "Error messages are API", CLI pitfall
- `../python-ftfy/ftfy/cli.py`
- `../python-ftfy/tests/test_cli.py` — acceptance tests (face.txt, sloppy-windows-1252, errors)

## Lore

- Flags: `filename`, `-o/--output`, `-g/--guess`, `-e/--encoding`, `-n/--normalization`,
  `--preserve-entities`. Exit code 1 on error; honor `os.EOL` line handling.
- Wrong-encoding error must match the windows-1252 charmap message verbatim (see DESIGN).
- The same-file guard (input == output) must error.
- Tests run against built `dist/bin.js`, not TS sources.

## Tasks

To be scoped when index-orchestration completes.
